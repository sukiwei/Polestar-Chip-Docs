site_name: Polestar Chip Docs

# 👇 关键变化：把主题换成了 material
theme:
  name: material
  language: zh  # 让界面变成中文

plugins:
  - search
  - with-pdf:
      author: Sukiwei
      copyright: Polestar Chip
      output_path: datasheet.pdf
      # 👇 关键修复：防止 Chrome 在服务器上崩溃
      headless_chrome_options:
        args:
          - --no-sandbox
          - --disable-gpu
          - --disable-dev-shm-usage
      # 👇 强制使用我们安装的中文字体
      render_js: false
      locale: zh_CN 