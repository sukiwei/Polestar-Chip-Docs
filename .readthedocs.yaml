version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
  # 👇 修改了这里：换成了更稳健的系统库组合
  apt_packages:
    - libcairo2
    - libpango-1.0-0
    - libpangocairo-1.0-0
    - libgdk-pixbuf2.0-0
    - shared-mime-info

python:
  install:
    - requirements: requirements.txt

mkdocs:
  configuration: mkdocs.yml